# ============================================
# PREMO API 환경변수 설정
# 이 파일을 .env로 복사하여 실제 값을 입력하세요
# ============================================

# === 인증 (필수) ===
JWT_SECRET=your-jwt-secret-key-minimum-32-characters-long
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=your-secure-admin-password
ADMIN_PASSWORD_HASH=  # 또는 PBKDF2 해시값 (선택)
ADMIN_BYPASS_PASSWORD=  # 개발용 바이패스 (선택)

# === Google OAuth (필수) ===
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# === Gemini API (필수) ===
GEMINI_API_KEY=AIzaSy...your-gemini-api-key

# === Notion API (선택) ===
NOTION_TOKEN=ntn_...your-notion-token

# === Power Automate (선택) ===
PA_EMAIL=your-email@domain.com
PA_PASSWORD=your-pa-password

# === Gmail (선택) ===
GMAIL_EMAIL=your-gmail@gmail.com
GMAIL_PASSWORD=your-app-password

# === Raspberry Pi SSH (선택) ===
PI_HOST=192.168.x.x
PI_USER=your-username
PI_PASS=your-ssh-password

# === Upstash Redis (선택) ===
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# === 기타 ===
NODE_ENV=development
